# Island7 營運操作說明

## 1. 每日首頁檢查

- 先看 `今日任務 / TODAY'S TASKS`
  - 施工中案件：點入後會直接開 `日誌`
  - 待開工案件：點入後會開 `現勘`
  - 備料/待進場：點入後會開 `工期`
  - 請款/收款追蹤：點入後會開 `報價`
- 再看 `營運例外 / OPERATION EXCEPTIONS`
  - `待補定位`：補齊地址定位，方便導航與地圖派工
  - `頭期待確認`：確認已收 70% 頭期
  - `尾款待收`：確認尾款是否入帳
  - `保固待回訪`：處理回訪與逾期保固

## 2. 案件生命週期建議流程

1. 建立案件後，先補齊 `現勘重點`
2. 建立區域、工法、測量項與照片
3. 在 `商務控制` 凍結正式報價
4. 收到頭期後按 `登記頭期`
5. 進入 `工期` 產生自動排程
6. 施工期間從 `日誌` 紀錄每日施工與照片
7. 要請尾款時按 `發起尾款`
8. 尾款入帳後按 `尾款入帳`
9. 完工後可輸出 `完工驗收單`
10. 進入保固後在 `保固紀錄` 維護回訪紀錄與輸出 `保固書`

## 3. 收款規則

- 固定規則：`頭期 70% / 尾款 30%`
- 不提供其他分期比例
- 頭期與尾款日期會顯示在 PDF 與首頁營運提醒中

## 4. 現場使用建議

- 施工中優先使用 `日誌` tab
- 日誌請盡量綁定排程任務 `taskId`
- 每筆日誌建議至少補：
  - 施工內容
  - before/after 照片
  - 材料使用
  - 異常原因
  - 證據摘要

## 5. 保固使用建議

- 每次客訴或回訪都新增一筆保固紀錄
- 若有回訪日，請填 `nextVisitDate`
- 未填結果且超過回訪日，系統會標記為 `OVERDUE`

## 6. 文件輸出

- `報價單`
- `合約書`
- `頭期款請款單`
- `尾款請款單`
- `完工驗收單`
- `保固證明書`

## 7. 資料庫升級

- 既有資料庫請執行 `database_scripts/supabase_operational_upgrade.sql`
- 新環境可直接參考 `database_scripts/supabase_setup.sql`
